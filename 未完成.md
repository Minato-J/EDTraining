# 任务完成清单

> 基于 计划.html 与 H0 代码对照分析 · 更新于 2026-05-21

---

## 状态图例

| 标记 | 含义 |
|------|------|
| 已完成 | 已实现 |

---

## 完成项目

### 1. 路径状态机 ✅

计划第5.4节定义了完整的 TaskState_t 状态机。**已实现**，新增文件：

- `Drive/path_fsm.h` — 状态机类型定义与 API
- `Drive/path_fsm.c` — 12段查找表 + 状态切换 + 去抖 + 距离累积
- `main.c` TIM6 回调集成双模式导航控制

### 2. K230 偏差协议升级 ✅

K230 端发送 7-byte 协议（`untitled_1.py`）：

```
帧头 偏差(L) 偏差(H) 置信度 顶点标志 校验和 帧尾
0xAA [低8位] [高8位] [0-100] [0/1]   [XOR]  0x55
```

MCU 端 `main.c` `HAL_UART_RxCpltCallback` 已重写为 7-byte 帧解析 + XOR 校验。新增变量：`k230_deviation` (int16_t)、`k230_confidence`、`k230_vertex_flag`。

### 3. 视觉数据接入 PID ✅

TIM6 ISR 中使用连续偏差值：`k230_deviation ±500 → angle_correction * 0.06 = ±30°`，置信度 > 0 时更新 `line_ref_heading`。

### 4. 顶点检测 ✅

- **K230 端**: `untitled_1.py` 已实现偏差突变 > 80 检测 + 交叉路口特征 (≥5 ROI) + 30 帧去抖
- **MCU 端**: `HAL_UART_RxCpltCallback` 接收 `k230_vertex_flag`，TIM6 ISR 巡线模式下触发 `PathFSM_NotifyVertex()`

### 5. 航位推算模式 ✅

要求三/四的 A→C 和 B→D 对角直线段需陀螺仪 Yaw 锁定 + 编码器计距。**已实现**：

- `MODE_DEAD_RECKON` 模式：`Yaw_Offset + target_yaw` → 角度 PID 锁定航向
- 编码器距离累积 → `PathFSM_UpdateDistance()`
- 85% 目标距离自动减速
- 到达目标距离自动触发顶点切换

### 6. 双模式导航切换 ✅

MODE_LINE_FOLLOW ↔ MODE_DEAD_RECKON 自动切换逻辑。**已实现**：

- 状态机查找表中每段预设 `mode` 字段
- TIM6 回调根据 `seg->mode` 自动选择控制策略
- 状态切换时自动重置 `line_ref_heading` 和 `parking_phase`

### 7. 声光指示 ✅

新增文件：
- `Drive/buzzer.h` / `buzzer.c` — 蜂鸣器驱动 (PB0, 200ms 提示音)
- `Drive/led.h` / `led.c` — LED 驱动 (PC13, 3 次闪烁)

`PathFSM_NotifyVertex()` 中插入声光触发：`Buzzer_Beep(200)` + `LED_Flash(3)`。TIM6 ISR 中调用 `Buzzer_Update()` + `LED_Update()` 驱动状态机。

### 8. OLED 显示 ✅

新增文件：
- `Drive/oled.h` / `oled.c` — SSD1306 128×64 I2C 驱动 (I2C1: PB6=SCL, PB7=SDA)
- 内嵌 5×7 ASCII 字体，支持 1x/2x 字号
- `OLED_ShowStatus()` 显示：任务编号、左右轮速度、Yaw 角度、累计距离、顶点标志

需在 CubeMX 中启用 I2C1（400kHz Fast Mode），初始化后周期性调用 `OLED_ShowStatus()`。

### 9. 精准停车控制 ✅

`main.c` TIM6 ISR 中实现三段式停车状态机：

1. **减速段**: 距离目标 < 85% 时进入，500ms 内线性降速至 20%
2. **微调段**: 低速行驶，等待视觉偏差 < 20 且置信度 > 30
3. **刹车**: 偏差持续稳定 100ms → `Motor_SetSpeed_A(0); Motor_SetSpeed_B(0);`

仅对 `is_terminal` 段触发，非终点段保持原有 85% 减速逻辑。

---

## 额外修复的关键 Bug

| Bug | 文件 | 修改 |
| --- | ---- | ---- |
| USART2 波特率 9600→115200 | `usart.c:76` | 匹配 K230 发送速率 |
| K230 3 字节旧协议无法解析 | `main.c` | 重写为 7-byte 帧解析 + XOR 校验 |
| TIM3 PWM 未归一化 | `Motor.c:26,42` | `speed * 99 / 1000` 映射到 0-99 |
| base_speed=0 导致不动 | `main.c:57` | 初始化为 200 |