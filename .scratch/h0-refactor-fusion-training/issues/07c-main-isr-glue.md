# Issue 07c: 集成层 — main.c TIM6 ISR 胶水代码

**Status:** `enhancement` `ready-for-agent`

**Created:** 2026-06-07

**Parent:** Issue 07 — car_control 依赖反转

**Feature:** h0-refactor-fusion-training

**Depends on:** Issue 07a + 07b（结构体和函数体已就位）

## 范围

仅改 `main.c` 的 `HAL_TIM_PeriodElapsedCallback` 中调用 `Car_ControlLoop` 的那一行。

## 改动

### main.c — ISR 中约第 331 行

```c
// ============ 旧（替换掉这一行）============
        Car_ControlLoop();

// ============ 新 ============
        Car_SensorInputs in = {current_angle, line_sensor_data, k230_data_valid};
        Car_ControlOutputs out = Car_ControlLoop(in);

        target_v_left  = out.target_v_left;
        target_v_right = out.target_v_right;
        if (out.emergency_stop) task_running = 0;

// ============ 后续 speed PID + Motor_SetSpeed 保持不变 ============
```

## 验证

1. `main.c` 中不再有裸调用 `Car_ControlLoop()`
2. `current_angle` / `line_sensor_data` / `k230_data_valid` 组装进 `Car_SensorInputs`
3. `target_v_left` / `target_v_right` 从 `out` 解包后赋值
4. `out.emergency_stop` 被检查并写入 `task_running`
5. Keil 编译零错误零警告
6. 逻辑行为与重构前完全等价——不改变任何运行时决策

---

> *This was generated by AI during triage.*
